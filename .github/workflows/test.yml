name: Sunucu Testi
on:
  push:
    branches: [ "main" ]

jobs:
  sistemi-test-et:
    runs-on: self-hosted
    steps:
      - name: Kodu Sunucuya Cek
        uses: actions/checkout@v4

      - name: Kaynak Durumunu Kontrol Et
        run: |
          echo "GitHub Actions senin sunucunda basariyla calisti!"
          echo "Sunucu zamani: $(date)"
          free -h
          df -h | grep '^/dev/'
